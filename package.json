{
  "name": "cse340-practice-platt",
  "dependencies": {
    "ejs": "^5.0.2",
    "express": "^5.2.1"
  },
  "type": "module",
  "scripts": {
    "dev": "kill-port 3000 && nodemon --legacy-watch --watch . --ext js,ejs,json --env-file=.env server.js",
    "start": "node --env-file=.env server.js",
    "kill": "kill-port 3000"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.14"
  }
}
